module Insert where

-- insertAt :: [a] > Int > a > [a]
-- insertAt [] _ a = [a]
-- insertAt xs 0 a = a : xs
-- insertAt (x:xs) n a | n < 0 || n > length xs = error "Index out of bounds"
--                     | otherwise = x insertAt xs (n - 1) a
                     